function test(variable) {
  
  
  if (str.length === 0) {
    return
  }

  // console.log('dddddd', str)
  // 首字母
  // let initial = str.charAt(0)
  let toArray = variable.split('')


  const reg = /[A-Z]/
  // let signle = str.split("")
  // str.split('').map((item, index) => {
  

    // console.log('item', item)













    // if (reg.test(item)) {
    //   // return str.slice(0, index)
    //   console.log('当前字符串', str.slice(0, index))
    //   console.log('')
    //   // console.log('剩余字符串', str.slice(index))
    //   let leftStr = str.slice(index)
    //   test(leftStr)
    // }
  // })
}

test('AnnouncementSupplierUploadDocumentDetail')